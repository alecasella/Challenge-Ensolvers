import React from 'react';
import '../css/App.css';
import {
    BrowserRouter as Router,
    Route, Routes,
} from 'react-router-dom';
import {
    Container,
} from 'reactstrap';

const Main = () => {
    return (
            <Container fluid>
                <Router>

                    <Routes>

                    </Routes>

                </Router>
            </Container>
    );
};

export default Main;